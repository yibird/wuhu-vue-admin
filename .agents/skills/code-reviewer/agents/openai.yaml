interface:
  display_name: "Code Reviewer"
  short_description: "Review frontend changes for defects"
  default_prompt: "Use $code-reviewer to review the current frontend changes for bugs, risks, and missing tests."
policy:
  allow_implicit_invocation: true
