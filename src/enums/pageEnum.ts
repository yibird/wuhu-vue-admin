export enum PageEnum {
  // basic login page path
  BASE_LOGIN = "/login",
  // basic home page path
  BASE_HOME = "/dashboard/console",
  // basic error page path
  ERROR_PAGE = "/exception",
  // error log page path
  ERROR_LOG_PAGE = "/error-log/list",
}
