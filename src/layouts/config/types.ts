export interface SearchModalProps {
  show?: boolean
}

export interface SearchModalEmits {
  (e: 'close'): void
}

export interface NoteBookModalProps {
  show?: boolean
}

export interface NoteBookModalEmits {
  (e: 'close'): void
}
