export interface TableSetting {
    // 是否显示序号列,默认true
    showIndexColumn?: boolean;
    // 是否显示table大小设置,默认true
    showTableSizeSetting?: boolean;
    // 是否显示选中列,默认true
    showSelectionColumn?: boolean;
    // 是否显示table 刷新,默认true
    showRefresh?: boolean;
}