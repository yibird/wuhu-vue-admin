/**
 * Re-export dnd-kit's single-element draggable composable.
 *
 * The composable must be called from a component rendered inside
 * `@dnd-kit/vue`'s `DragDropProvider`.
 */
export { useDraggable } from '@dnd-kit/vue'
export type { UseDraggableInput } from '@dnd-kit/vue'
